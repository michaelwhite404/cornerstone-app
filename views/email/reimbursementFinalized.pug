extends baseEmail

block content
  - const status = data.reimbursement.approval.approved ? "Approved" : "Rejected";
  table(style="padding-bottom:25px" role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          p Hi #{firstName},
          p Your reimbursement request has been 
            strong #{status}
  include ./components/reimbursementInfo.pug
  +ctaButton View Reimbursement